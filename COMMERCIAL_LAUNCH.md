# ClipFlow 商用上线说明

## 当前已落地的商用 MVP

- 套餐：免费版、标准版、高级版。
- 游客体验：免注册 2 次，单链接体验，额度用完后引导注册。
- 免费版：每日 5 次、每月 150 次、单次最多 2 个链接。
- 标准版：19.9 元/月，每月 1500 次、无每日限制、单次最多 5 个链接。
- 高级版：99 元/月，每月 10000 次、单次最多 5 个链接、优先解析通道和专属支持。
- 额度：解析按链接次数扣减，AI 改写按次数扣减，用户中心展示本月/今日剩余额度。
- 账号：普通用户只能看个人中心；管理员账号可进入用户管理页。
- 管理员：支持搜索用户、启用/禁用用户、调整普通用户套餐、查看用量和近期失败原因。
- 账单：当前为手动收款模式，会记录待支付订单；接入支付后可替换为真实支付链接。
- 合规：页脚提供服务条款、隐私政策、版权声明、删除数据、联系方式入口。
- 安全：生产环境 Cookie 自动增加 `Secure`；注册密码要求至少 8 位并包含字母和数字。

## 上线前必须替换的生产能力

1. 数据库
   - 当前仍使用 `data/users.json`，只适合本地或小规模试运行。
   - 生产建议迁移 PostgreSQL，表结构至少包含：
     - `users`
     - `sessions`
     - `jobs`
     - `assets`
     - `usage_records`
     - `subscriptions`
     - `payments`
   - 已提供生产表结构：`database/schema.sql`。

2. 队列
   - 当前任务仍在 Node 进程内执行。
   - 多用户商用建议使用 Redis + BullMQ，把解析、下载、转码、转写、AI 改写拆成后台队列。

3. 对象存储
   - 当前文件存放在 `tmp/jobs`，会按任务 TTL 清理。
   - 生产建议使用 Cloudflare R2、阿里云 OSS、腾讯云 COS 或 S3。

4. 支付
   - 当前 `PAYMENT_PROVIDER=manual` 只创建手动待支付记录。
   - 国内商用建议接微信支付 + 支付宝，快速验证可先接聚合支付。
   - 海外用户再接 Stripe Checkout。

5. Provider
   - 免费解析接口稳定性和商用条款不可控。
   - 正式收费前需要换成明确允许商用、有 SLA 或自有授权的数据 Provider。

## 推荐上线步骤

1. 配置生产环境变量
   - `NODE_ENV=production`
   - `AUTH_SECRET` 使用 32 字节以上随机密钥
   - `DEFAULT_ADMIN_EMAIL`
   - `DEFAULT_ADMIN_PASSWORD`
   - `DATABASE_URL`
   - `REDIS_URL`
   - `STORAGE_PROVIDER`
   - `PAYMENT_PROVIDER`

2. 购买域名并配置 HTTPS
   - Nginx / Caddy / 云厂商负载均衡都可以。
   - Cookie Secure 依赖 HTTPS。

3. 迁移数据存储
   - 先把 JSON 用户导入 PostgreSQL。
   - 再把任务、资产、用量、支付拆表。

4. 接入支付回调
   - 创建订单后返回支付链接。
   - 支付成功回调必须验签。
   - 回调幂等处理，避免重复开通套餐。

5. 配置监控
   - 记录 Provider 成功率、失败原因、平均耗时。
   - 记录队列长度、转码失败率、对象存储上传失败率。
   - 配置错误告警和每日备份。

## 当前默认管理员

- 邮箱：`admin@clipflow.local`
- 密码：`Admin@123456`

生产上线前必须在 `.env` 中改成你自己的管理员邮箱和强密码。
